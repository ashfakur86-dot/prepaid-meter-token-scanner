name: Ultimate Flutter APK Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    - name: Fix Android MinSdkVersion
      run: |
        sed -i 's/minSdkVersion .*/minSdkVersion 21/' app/android/app/build.gradle

    - name: Get Dependencies
      run: |
        cd app
        flutter pub get

    - name: Build APK Release
      run: |
        cd app
        flutter build apk --release

    - name: Upload Finished Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Prepaid-Token-App
        path: app/build/app/outputs/apk/release/app-release.apk
